<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aPlatform.mappers.FinanceDataMapper">
	<select resultType="com.aPlatform.controller.service.finance.VO.FinanceVO" id="getAllCode">
		SELECT 
    		CODE 				AS "finCode"
    		, NAME 				AS "finName"
		FROM	 
  		  SHARES_FIN_INFO
	</select>
	<select resultType="com.aPlatform.controller.service.finance.VO.FinanceVO" id="getMappingUrl">
		SELECT 
		    SUB_CUL_1			AS "finType"
		    , SUB_CUL_2			AS "finUrl"
		FROM
		    COM_CD_BAS
		WHERE
		    GROUP_ID = '20'
		    AND SERVICE_ID		= 'FIN'
		    AND SUB_CONTENT		= 'url'
	</select>
	<select resultType="com.aPlatform.controller.service.finance.VO.FinanceVO" id="getMappingPharse">
		SELECT 
		    SUB_CUL_1			AS "finType"
		    , SUB_CUL_2			AS "finPharse"
		FROM
		    COM_CD_BAS
		WHERE
		    GROUP_ID = '20'
		    AND SERVICE_ID		= 'FIN'
		    AND SUB_CONTENT		= 'pharse'
	</select>
	<insert id="insertSharesInfo" parameterType="com.aPlatform.controller.service.finance.VO.FinanceVO">
		INSERT INTO
			SHARES_FIN_INFO
			(
				CODE
				, NAME
				, EXCHANGE
			)
			VALUES
			(
				#{finCode}
				, #{finName}
				, #{exchange}
			)
	</insert>
	<delete id="deleteAllDataInShareTable">
		DELETE FROM SHARES_FIN_INFO
	</delete>
	<insert>
	</insert>
</mapper>